import java.io.*;
import java.util.*;

public class Day {
    public static String dayOfWeek( int dayInMonth, String dayOfFirst )
    {
        String[] daysOfWeek = {"Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"};
        int daysToGo = dayInMonth % 7;
        int days;
        String dayToReturn = "";
        for (days = 0, days <= 6, days++) {
            if (daysOfWeek[days] == dayOfFirst) {
                dayToReturn +=
            }
        }

    }
}